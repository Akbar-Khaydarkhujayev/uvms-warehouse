import { DashboardContent } from 'src/layouts/dashboard';

// ----------------------------------------------------------------------

export function ArendatorsView() {
  return (
    <DashboardContent
      maxWidth="xl"
      sx={{ p: 2, maxHeight: 'calc(100cvh - 80px)', overflow: 'hidden' }}
    >
      ARENDATROS
    </DashboardContent>
  );
}
